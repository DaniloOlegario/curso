// criado após a instalacão de um package para alterar o tema, cor e fonte do aplicativo

import 'package:flutter/material.dart';

final class UiConfig {
  UiConfig._();

  static const primaryColor = Color.fromARGB(255, 65, 30, 192);
  static const secondaryColor = Color(0XFFDA251C);

  static final theme = ThemeData();
}
